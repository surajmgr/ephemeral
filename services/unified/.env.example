# Service Control
# Options: typesense, novu, minio (comma separated)
RUN_SERVICES=typesense,novu

# --- Typesense ---
TYPESENSE_API_KEY=xyz
# Data dir is fixed to /data/typesense in the container

# --- Minio (S3 Compatible) ---
# Removed in favor of Backblaze B2

# --- Novu ---
# Secrets
JWT_SECRET=super-secret-jwt-key
STORE_ENCRYPTION_KEY=32-char-encryption-key-must-be-long

# Hostname (for clients to reach Novu)
HOST_NAME=http://localhost:4200
REACT_APP_API_URL=http://localhost:3000
REACT_APP_WS_URL=http://localhost:3002

# Database (External Required)
# Example: mongodb://user:pass@mongo-host:27017/novu
MONGO_URL=
# Example: redis://redis-host:6379
REDIS_URL=

# --- Backblaze B2 (Required) ---
# S3_LOCAL_STACK is not set to disable localstack
S3_BUCKET_NAME=your-b2-bucket
S3_REGION=us-east-005
AWS_ACCESS_KEY_ID=your-key-id
AWS_SECRET_ACCESS_KEY=your-app-key
# For B2, the endpoint is typically: https://s3.us-east-005.backblazeb2.com
S3_ENDPOINT=https://s3.us-east-005.backblazeb2.com

# Ports (Mapped in Docker)
# 3000: API
# 3002: WebSocket
# 4200: Web
# 8108: Typesense
