# Typesense Config
TYPESENSE_API_KEY=xyz
TYPESENSE_HOST=http://localhost:8108

# Backup Config
BACKUP_INTERVAL=300 # 5 minutes

# Cloud Config (Rclone - pCloud)
# 1. Run 'rclone config' locally to generate the config for pCloud
# 2. View the config usually in ~/.config/rclone/rclone.conf
# 3. Extract the values.
#
# RCLONE_CONFIG_REMOTE_TYPE should be 'pcloud'
RCLONE_CONFIG_REMOTE_TYPE=pcloud
# The 'token' is a JSON string. Ensure it is properly quoted if needed in your environment (e.g. single quotes in docker-compose)
# Northflank Secret Tip: Paste the JSON content directly as the value.
RCLONE_CONFIG_REMOTE_TOKEN={"access_token":"...","token_type":"Bearer",...}
# Optional: Hostname if using EU/US specific pCloud region if needed (usually handled by token/rclone defaults, but check rclone docs)
# RCLONE_CONFIG_REMOTE_HOSTNAME=api.pcloud.com 

CLOUD_DEST=remote:backups/typesense
BACKUP_RETENTION_COUNT=10
